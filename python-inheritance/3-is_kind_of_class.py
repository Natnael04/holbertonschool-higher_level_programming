#!/usr/bin/python3
"""check if the object is an instance or instance of a class"""


def is_kind_of_class(obj, a_class):
    """check if the object is an instance or instance of a class"""
    return isinstance(obj, a_class)
